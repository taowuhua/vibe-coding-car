# =====================================================
# Car Cloud - Config 配置中心
# 预生产环境配置 (preprod)
# =====================================================

server:
  port: 8847

spring:
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/your-repo/car-config-repo.git
          default-label: preprod
          username: ${GIT_USERNAME:}
          password: ${GIT_PASSWORD:}

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://eureka-preprod.car.local:8761/eureka/

logging:
  level:
    root: WARN
