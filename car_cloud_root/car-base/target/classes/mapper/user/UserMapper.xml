<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.car.base.mapper.user.UserMapper">

    <!-- 根据用户名查询用户 -->
    <select id="findByUsername" resultType="com.car.base.entity.user.User">
        SELECT * FROM users WHERE username = #{username}
    </select>

    <!-- 如果表不存在则创建用户表 -->
    <update id="createTableIfNotExists">
        CREATE TABLE IF NOT EXISTS users (
            id BIGINT AUTO_INCREMENT PRIMARY KEY,
            username VARCHAR(255) NOT NULL UNIQUE,
            password VARCHAR(255) NOT NULL
        )
    </update>

    <!-- 插入用户数据，并返回自增主键 -->
    <insert id="insert" parameterType="com.car.base.entity.user.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, password) VALUES (#{username}, #{password})
    </insert>

</mapper>
