# =====================================================
# Car Cloud - Job 定时任务服务
# 预生产环境配置 (preprod)
# =====================================================

server:
  port: 9003

# Nacos 服务发现配置
spring:
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_SERVER:nacos-preprod.car.local:8848}
        namespace: preprod

logging:
  level:
    root: WARN
