# =====================================================
# Car Cloud - Job 定时任务服务
# 主配置文件 - 公共配置
# =====================================================

spring:
  application:
    name: car-job
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

# 监控端点配置
management:
  endpoints:
    web:
      exposure:
        include: "*"
