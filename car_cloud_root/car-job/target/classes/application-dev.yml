# =====================================================
# Car Cloud - Job 定时任务服务
# 开发环境配置 (dev)
# =====================================================

server:
  port: 9003

# Nacos 服务发现配置
spring:
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: car_dev

logging:
  level:
    root: INFO
    com.car: DEBUG
