# =====================================================
# Car Cloud - Job 定时任务服务
# 开发环境配置 (dev)
# =====================================================

server:
  port: 9003

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

logging:
  level:
    root: INFO
    com.car: DEBUG
